Can you create a python file with the transformer, another one with the lstm, another one with the xLstm,another one with the pareto nbd ?
- Each file should contain its optuna optimization (except pareto NBD)
- Each file would take as input the train_loader, val_loader, N_covariates
